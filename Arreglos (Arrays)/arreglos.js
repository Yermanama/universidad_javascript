//let autos = new array('BMW', 'Mercedes', 'Volvo'); // Esta es la sintaxis clásica, con el operador array, pero ya no se recomienda

const autos = ['BMW', 'Mercedes', 'Volvo'] // Con const creamos un espacio de memoria que no se va a poder cambiar, pero el interior de ese objeto si que se va a poder cambiar
console.log(autos)