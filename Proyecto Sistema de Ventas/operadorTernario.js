let resultado = 3>2 ? "verdadero" : "falso"
console.log(resultado)

// Dada la condición asigna el primer valor si es verdadero, sino se aplica el segundo valor

// Expresión ? valorSiVerdadero : valorSiFalso

let numero = 2

let esPar = numero % 2 == 0 ? "Es par" : "Es impar"
console.log(esPar)