let a = 15
let valorMinimo = 0, valorMaximo = 10

if (a >= valorMinimo && a <= valorMaximo){ // Es el operador AND, es decir, ambas expresiones deben de regresar verdadero
    console.log("Valor dentro de rango")
} else {
    console.log("Valor fuera de rango.")
}
